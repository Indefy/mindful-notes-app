{
  "name": "mindful-notes-app",
  "version": "1.0.0",
  "description": "A modern note-taking app focused on mental wellness.",
  "main": "src/index.js",
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "react-scripts eject"
  },
  "keywords": [
    "note-taking",
    "mental wellness",
    "react",
    "tailwindcss"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "lucide-react": "^0.456.0",
    "tailwindcss": "^2.2.19"
  },
  "devDependencies": {
    "postcss": "^8.3.6",
    "autoprefixer": "^10.3.7"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
